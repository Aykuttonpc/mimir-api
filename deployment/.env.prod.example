# InstaClone Production Environment — TEMPLATE
# Gerçek değerleri /opt/instaclone/.env.prod dosyasına koy. Bu dosyayı KESİNLİKLE git'e commit etme.

# --- Database ---
DB_USER=instaclone
DB_PASSWORD=                      # 32+ karakter random; openssl rand -base64 32
DB_NAME=instaclone

# --- Redis ---
REDIS_PASSWORD=                   # 32+ karakter random; openssl rand -base64 32

# --- Web image ---
WEB_TAG=                          # T-007 sonrası: semver (v0.1.0) veya commit SHA. 'latest' YASAK.

# --- JWT ---
JWT_KEY=                          # 64+ karakter random; openssl rand -base64 64
JWT_ISSUER=instaclone-api
JWT_AUDIENCE=instaclone-mobile

# --- SMS provider (T-010 sonrası seçilecek) ---
SMS_PROVIDER=                     # netgsm | twilio | vonage
SMS_API_KEY=

# --- SMTP (email verification) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
